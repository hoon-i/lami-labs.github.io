# 멤버 사진 폴더

- 기본 사진: 이 폴더에 `<slug>.jpg` (예: `jeonghun-hong.jpg`)
- hover 시 바뀌는 사진(선택): `hidden/<slug>.jpg`
- `<slug>`는 해당 멤버 YAML(`data/members/<과정>/<slug>.yaml`)의 `slug` 값과 같아야 합니다.
- 3:4 세로 비율 권장. jpg 기준이며 다른 확장자를 쓰려면 YAML에 `image: 파일명.png`를 적습니다.
